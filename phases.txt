**Phase 2 — School Management** (Super Admin side)

**Step 1: School Model + Controller + Routes (backend)**
- `schoolController.js` — Super Admin functions:
  - `getPendingSchools` — list schools awaiting approval
  - `approveSchool` / `rejectSchool`
  - `getAllSchools` — list active schools
  - `getSchoolStats` — dashboard numbers (total schools, pending, revenue etc.)
- `schoolRoutes.js` — wire these up, protected with `authorize("superadmin")`

**Step 2: Super Admin Dashboard (frontend)**
- Matches your "System Overview" ClassPilot design — stats cards, incoming requests table, approve/reject buttons

**Step 3: Then move to School Admin flow**
- Class model + controller
- Student model + controller
- Teacher assignment to class

